<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_logo_glow"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_logo_glow"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalFinance"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Splash Activity - Launcher -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PersonalFinance.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalFinance.NoActionBar" />

        <!-- Register Activity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalFinance.NoActionBar" />

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalFinance" />

        <!-- Scan Bill Activity -->
        <activity
            android:name=".activities.ScanBillActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalFinance.NoActionBar" />

        <!-- Scan Product Activity -->
        <activity
            android:name=".activities.ScanProductActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalFinance.NoActionBar" />

        <!-- Category Limit Details Activity -->
        <activity
            android:name=".fragments.category.CategoryLimitDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.PersonalFinance" />

        <!-- Facebook SDK Meta Data -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <!-- Facebook Login Activity -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

    </application>

</manifest>
